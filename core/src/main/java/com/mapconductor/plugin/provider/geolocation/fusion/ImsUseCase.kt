package com.mapconductor.plugin.provider.geolocation.fusion

/**
 * High-level motion profile presets for IMS (IMU + GPS) fusion tuning.
 */
enum class ImsUseCase {
    WALK,
    BIKE,
    CAR,
    TRAIN
}

